#include <stdio.h>

int main(int argc, char**argv)
{
    char c;
    while(1)
    {
        scanf("%c", &c);
        if (c=='.') break;
        switch (c)
        {
            case 'a':
                printf("%c",
    }
    
    return 0;
}

